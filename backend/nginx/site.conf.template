server {
    listen 80;
    listen [::]:80;
    server_name __SERVER_NAME__;

    root __DEPLOY_PATH__/current;
    index index.html;

    location / {
        try_files $uri $uri/ $uri.html =404;
    }
}
